# tex-server — workspace-to-port mapping (http-level, outside server block)
# Mirrors config.ini [workspaces]
map $tex_workspace $tex_port {
    static   7777;
    alpha    7771;
    beta     7772;
    gamma    7773;
    delta    7774;
    epsilon  7775;
    default  7777;
}
